# 🔄 Автоматические миграции базы данных

## Как это работает

При запуске бота через Docker Compose миграции применяются **автоматически**:

```
docker compose up
    ↓
Запускается контейнер bot
    ↓
Выполняется entrypoint.sh
    ↓
1. Ожидание готовности PostgreSQL
2. Автоматическое применение миграций (alembic upgrade head)
3. Запуск бота
```

## Что происходит внутри

### entrypoint.sh

```bash
#!/bin/sh
set -e

echo "🚀 Starting Otmazochnik Bot..."

# Ждем пока PostgreSQL станет доступна
echo "⏳ Waiting for PostgreSQL to be ready..."
while ! nc -z postgres 5432; do
  sleep 1
done
echo "✅ PostgreSQL is ready!"

# Применяем миграции базы данных
echo "🗄️  Running database migrations..."
alembic upgrade head
echo "✅ Migrations applied successfully!"

# Запускаем бота
echo "🤖 Starting bot..."
exec python -m app.main
```

### Что происходит:

1. **Проверка PostgreSQL** - скрипт ждет пока база данных станет доступна
2. **Применение миграций** - автоматически выполняется `alembic upgrade head`
3. **Запуск бота** - после успешных миграций запускается основное приложение

## Для пользователя

**Вам не нужно ничего делать вручную!**

Просто:
```bash
# 1. Настройте .env файл
cp env.example .env
nano .env  # Добавьте TELEGRAM_BOT_TOKEN и OPENROUTER_API_KEY

# 2. Запустите бота
docker compose up -d

# Все миграции применятся автоматически!
```

## Логи миграций

Проверить применение миграций:

```bash
docker compose logs bot

# Вы увидите:
# 🚀 Starting Otmazochnik Bot...
# ⏳ Waiting for PostgreSQL to be ready...
# ✅ PostgreSQL is ready!
# 🗄️  Running database migrations...
# INFO  [alembic.runtime.migration] Context impl PostgresqlImpl.
# INFO  [alembic.runtime.migration] Will assume transactional DDL.
# INFO  [alembic.runtime.migration] Running upgrade  -> 001, Initial tables
# ✅ Migrations applied successfully!
# 🤖 Starting bot...
```

## Обновление структуры БД

Если вы разработчик и хотите добавить новые таблицы/поля:

```bash
# 1. Измените модели в app/models.py
# 2. Создайте новую миграцию
docker compose exec bot alembic revision --autogenerate -m "Add new field"

# 3. Перезапустите бота
docker compose restart bot

# Новая миграция применится автоматически при старте!
```

## Ручное управление миграциями (опционально)

Если нужен контроль:

```bash
# Войти в контейнер
docker compose exec bot sh

# Проверить текущую версию БД
alembic current

# Посмотреть историю миграций
alembic history

# Применить конкретную миграцию
alembic upgrade <revision_id>

# Откатить миграцию
alembic downgrade -1

# Откатить все
alembic downgrade base
```

## Локальная разработка без Docker

Если запускаете без Docker:

```bash
# Убедитесь что PostgreSQL запущен
# Примените миграции вручную
alembic upgrade head

# Запустите бота
python -m app.main
```

## Резервное копирование перед миграциями

Для production рекомендуется:

```bash
# Сделать backup перед обновлением
docker compose exec postgres pg_dump -U botuser otmazochnik > backup_$(date +%Y%m%d).sql

# Обновить код и перезапустить
git pull
docker compose up -d --build

# Миграции применятся автоматически

# Если что-то пошло не так - восстановить
docker compose exec -T postgres psql -U botuser otmazochnik < backup_20250108.sql
```

## Безопасность

entrypoint.sh использует `set -e` - если миграция упадет, бот не запустится. Это предотвращает запуск с несовместимой схемой БД.

Если увидите:
```
ERROR [alembic.runtime.migration] Can't locate revision identified by...
```

Значит в БД есть миграции, которых нет в коде. Нужно синхронизировать:
```bash
# Откатить БД к известной версии
docker compose exec bot alembic downgrade base

# Перезапустить
docker compose restart bot
```

---

**Итог:** Полностью автоматическая система миграций. Меняй только .env и запускай!
